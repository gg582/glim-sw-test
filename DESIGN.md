## 왜 하필 전략 패턴과 빌더 패턴이죠?

### 전략 패턴

전략 패턴을 사용한 이유는 필터 자체를 교체 가능하게 만들기 위해서입니다. 예를 들어 Resize의 보간법이나 Grayscale 변환 시 사용하는 색공간이나 가중치 정책은 나중에 변경될 수 있습니다. 호출부를 수정하지 않고도 알고리즘을 교체할 수 있도록 동일 인터페이스 뒤에 배치했습니다.

##  빌더 패턴

Builder를 둔 이유는 파이프라인 문자열을 실제 필터 집합으로 변환하는 책임을 한 곳에 모으기 위해서입니다. 원하는 결과가 나오지 않을 경우 파서 전체가 아니라 Builder 주변부터 확인할 수 있어 디버깅 범위가 줄어듭니다.

고등학교 때는 C로 BMP를 직접 처리하면서 ASCII 렌더링, 프레임버퍼 조작, 색공간 처리 등을 소수의 파일 안에 섞어서 작성한 적이 있습니다. 기능이 늘어날수록 결합도가 높아지고 수정 범위가 커지는 문제를 경험했습니다. 이후 동아리와 연구보조원 활동을 하면서 역할 분리와 책임 분리의 중요성을 배웠고, 이번 과제에도 그 경험을 반영했습니다.

### 그렇다면 왜 BMP는 전략 패턴으로 빼지 않았죠?

이미지 포맷의 경우 다양한 포맷이 있으며 그 처리 방식에 차이가 있습니다.
따라서 입력 포맷이 단일 확장자가 아니라면 `ImageFormatProcessor`와 같은 것을 두어, 해당 클래스에 BMP, TIFF 등을 주입할 수도 있습니다.

하지만, 문제의 제약 조건은 철저히 주어진 BMP 처리 소스 하에서 처리할 것을 명시합니다.
따라서, 이미 주어진 라이브러리와 제약 조건이 명확한데 추가적인 디자인 패턴을 적용하려고 하는 것은 과설계라고 판단했습니다.

### 갑자기 TIFF를 지원하라고 하면요?

현재 구조에서 사용되는 BMP 파서는 주입을 위한 구체 클래스로 격하한 후, 다양한 포맷을 지원하기 위한 추상 클래스를 만들 것입니다. 따라서 구조는 `ImageFormatProcessor` 아래에 `BmpProcessor`, `TIFFProcessor` 등이 주입된느 구조가 됩니다.
